<section class="section b-b">
    <div class="container">
        <div class="section-heading">
            <h2 id="examples">Hoverable rows</h2>
            <p class="lead">Add <code>.table-hover</code> to enable a hover state on table rows within a <code>tbody</code>.</p>
        </div>

        {% set table = { class: "table-hover" } %}
        {% include "~components/tables/_table.nunjucks" %}
    </div>
</section>
